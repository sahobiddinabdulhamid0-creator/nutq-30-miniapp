{
  "name": "nutq-30-telegram-miniapp",
  "version": "2.0.0",
  "description": "Nutq 30 — ikki foydalanuvchi uchun 30 kunlik audio nutq mashq platformasi",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js",
    "check": "node --check server/server.js && node --check server/geminiService.js && node --check public/js/app.js && node --check public/js/screens.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1137.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1"
  }
}
