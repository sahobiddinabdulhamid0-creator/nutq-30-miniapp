{
  "name": "nutq-30-telegram-miniapp",
  "version": "2.4.0",
  "description": "Nutq 30 — ikki foydalanuvchi uchun 30 kunlik audio nutq mashq platformasi",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js",
    "check": "node --check server/server.js && node --check server/auth.js && node --check server/geminiService.js && node --check server/liveTranscriptionGateway.js && node --check public/js/liveTranscriber.js && node --check public/js/audioRecorder.js && node --check public/js/app.js && node --check public/js/screens.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1137.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1",
    "ws": "^8.21.3"
  }
}
